{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Go build: windows GUI",
            "type": "shell",
            "command": "go build -ldflags=-H=windowsgui",
            "group": {
                "kind": "build",
                "isDefault": true
            },
        },
    ]
}